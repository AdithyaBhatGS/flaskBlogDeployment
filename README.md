# flaskBlogDeployment
A simple CRUD app built using Flask. The goal of the project is to learn containerization by containerizing it and pushing it to ECR and finally running it on ECS.  Also automating the deployment using Terraform and Jenkins.
